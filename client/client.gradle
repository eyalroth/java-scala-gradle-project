description = 'client - a java only module'
dependencies {
    implementation project(":common")
    implementation group: 'com.google.guava', name: 'guava'
}

applyFile("app")
configureApp('client', 'org.myproject.client.ClientHelloWorld')