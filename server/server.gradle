description = 'server - a mixed scala / java module'

applyFile("publish")

dependencies {
    implementation project(":common")
    implementation group: 'com.google.guava', name: 'guava'
}

applyFile("scala")

applyFile("app")
configureApp('server', 'org.myproject.server.ServerHelloWorld')