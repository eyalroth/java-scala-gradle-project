ext.applyFile = { name ->
    apply from: new File(rootProject.projectDir, "gradle/${name}.gradle")
}

ext.configureRepositories = { obj ->
    configure(obj) {
        repositories {
            jcenter()
        }
    }
}